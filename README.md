# Watt Bruno Collections

Each directory in the project is a Bruno collection. Open them with `Top left->File->Open Collection` then choose the main directory (`ACS/Lab Test Backend/DSAdmin REST`)
