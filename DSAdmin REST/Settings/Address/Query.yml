info:
  name: Query
  type: http
  seq: 2

http:
  method: GET
  url: http://{{hostname}}:8002/api/setting/address/1000
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
