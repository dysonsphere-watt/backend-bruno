info:
  name: Query
  type: http
  seq: 3

http:
  method: GET
  url: http://{{hostname}}:8002/api/setting/giftcard/query?key_word=Ball
  params:
    - name: key_word
      value: Ball
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
