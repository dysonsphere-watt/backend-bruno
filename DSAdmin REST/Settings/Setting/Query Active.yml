info:
  name: Query Active
  type: http
  seq: 2

http:
  method: GET
  url: "{{protocol}}://{{hostname}}:{{setting_port}}/api/setting/setting/:key"
  params:
    - name: key
      value: ADV_SLOT_CYCLE_NUM
      type: path
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
