info:
  name: Key Vault Secrets
  type: http
  seq: 5

http:
  method: GET
  url: "{{protocol}}://{{hostname}}:{{setting_port}}/api/setting/az-vault"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
