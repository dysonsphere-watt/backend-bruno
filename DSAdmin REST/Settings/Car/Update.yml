info:
  name: Update
  type: http
  seq: 4

http:
  method: PUT
  url: "{{protocol}}://{{hostname}}:{{setting_port}}/api/setting/car/:id"
  params:
    - name: id
      value: "4"
      type: path
  body:
    type: json
    data: "{\r

      \    \"car\": {\r

      \        \"model\": \"numodel\"\r

      \    }\r

      }"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
