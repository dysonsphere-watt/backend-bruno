info:
  name: Health Check
  type: http
  seq: 6

http:
  method: GET
  url: http://{{hostname}}:8002/api/setting/healthcheck
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
