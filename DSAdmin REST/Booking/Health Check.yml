info:
  name: Health Check
  type: http
  seq: 2

http:
  method: GET
  url: http://{{hostname}}:{{booking_port}}/api/booking/healthcheck
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
