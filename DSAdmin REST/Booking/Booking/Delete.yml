info:
  name: Delete
  type: http
  seq: 6

http:
  method: DELETE
  url: http://{{hostname}}:{{booking_port}}/api/booking/3
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
