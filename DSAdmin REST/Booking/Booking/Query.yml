info:
  name: Query
  type: http
  seq: 4

http:
  method: GET
  url: http://{{hostname}}:{{booking_port}}/api/booking/1454
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
