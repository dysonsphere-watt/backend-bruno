info:
  name: Generate Access Token
  type: http
  seq: 12

http:
  method: GET
  url: "{{protocol}}://{{hostname}}:{{booking_port}}/api/booking/:id/access-token"
  params:
    - name: id
      value: "285"
      type: path
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
