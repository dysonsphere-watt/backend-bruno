info:
  name: Ready for Charge
  type: http
  seq: 5

http:
  method: POST
  url: "{{protocol}}://{{hostname}}:{{booking_port}}/api/booking/:id/charge/ready"
  params:
    - name: id
      value: "2406"
      type: path
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
