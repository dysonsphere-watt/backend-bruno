info:
  name: Cancel Charging
  type: http
  seq: 5

http:
  method: PATCH
  url: "{{protocol}}://{{hostname}}:{{booking_port}}/api/booking/:id/charge/cancel"
  params:
    - name: id
      value: "182"
      type: path
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
