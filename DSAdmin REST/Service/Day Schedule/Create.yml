info:
  name: Create
  type: http
  seq: 1

http:
  method: POST
  url: http://{{hostname}}:{{service_port}}/api/service/day-schedule
  body:
    type: json
    data: "{\r

      \    \"type\": \"day schedule 2?\"\r

      }"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
