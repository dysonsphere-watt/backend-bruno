info:
  name: Delete
  type: http
  seq: 3

http:
  method: DELETE
  url: http://{{hostname}}:{{service_port}}/api/service/type/5
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
