info:
  name: Query
  type: http
  seq: 2

http:
  method: GET
  url: http://{{hostname}}:{{service_port}}/api/service/type
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
