info:
  name: Create
  type: http
  seq: 1

http:
  method: POST
  url: http://{{hostname}}:{{service_port}}/api/service/type
  body:
    type: json
    data: "{\r

      \    \"type\": \"Reserve Parking\"\r

      }"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
