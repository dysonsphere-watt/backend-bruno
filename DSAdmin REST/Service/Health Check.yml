info:
  name: Health Check
  type: http
  seq: 5

http:
  method: GET
  url: http://{{hostname}}:{{service_port}}/api/service/healthcheck
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
