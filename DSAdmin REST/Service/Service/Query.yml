info:
  name: Query
  type: http
  seq: 2

http:
  method: GET
  url: "{{protocol}}://{{hostname}}:{{service_port}}/api/service/:id"
  params:
    - name: id
      value: "7"
      type: path
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
