info:
  name: Get Price
  type: http
  seq: 6

http:
  method: GET
  url: "{{protocol}}://{{hostname}}:{{service_port}}/api/service/:service_id/price?duration=60&station_id=7005"
  params:
    - name: duration
      value: "60"
      type: query
    - name: station_id
      value: "7005"
      type: query
    - name: coupon_code
      value: test-30-cpn-1
      type: query
      disabled: true
    - name: service_id
      value: "24"
      type: path
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
