info:
  name: Check
  type: http
  seq: 7

http:
  method: GET
  url: "{{protocol}}://{{hostname}}:{{service_port}}/api/service/coupon/check?user_id=22&coupon_code=test-30-cpn-1"
  params:
    - name: user_id
      value: "22"
      type: query
    - name: coupon_code
      value: test-30-cpn-1
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
