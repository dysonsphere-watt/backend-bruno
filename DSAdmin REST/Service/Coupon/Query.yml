info:
  name: Query
  type: http
  seq: 2

http:
  method: GET
  url: "{{protocol}}://{{hostname}}:{{service_port}}/api/service/coupon/23"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
