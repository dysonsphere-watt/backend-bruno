info:
  name: Health Check
  type: http
  seq: 8

http:
  method: GET
  url: "{{protocol}}://{{hostname}}:{{robot_port}}/api/robot/healthcheck"
  headers:
    - name: Origin
      value: https://localhost:3002
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
