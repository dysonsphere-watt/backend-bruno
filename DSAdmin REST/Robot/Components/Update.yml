info:
  name: Update
  type: http
  seq: 3

http:
  method: PUT
  url: "{{protocol}}://{{hostname}}:{{robot_port}}/api/robot/component/:id"
  params:
    - name: id
      value: "1531"
      type: path
  body:
    type: json
    data: "{\r

      \    \"robot_component\": {\r

      \        \"status\": 100\r

      \    }\r

      }"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
