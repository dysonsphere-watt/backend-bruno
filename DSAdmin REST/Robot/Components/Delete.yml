info:
  name: Delete
  type: http
  seq: 4

http:
  method: DELETE
  url: "{{protocol}}://{{hostname}}:{{robot_port}}/api/robot/component/:id"
  params:
    - name: id
      value: "1531"
      type: path
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
