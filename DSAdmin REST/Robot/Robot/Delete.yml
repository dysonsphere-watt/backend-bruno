info:
  name: Delete
  type: http
  seq: 4

http:
  method: DELETE
  url: http://{{hostname}}:{{robot_port}}/api/robot/10016
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
