info:
  name: Query
  type: http
  seq: 2

http:
  method: GET
  url: http://{{hostname}}:{{robot_port}}/api/robot/7001
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
