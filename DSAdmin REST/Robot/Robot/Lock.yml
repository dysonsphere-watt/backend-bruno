info:
  name: Lock
  type: http
  seq: 10

http:
  method: POST
  url: "{{protocol}}://{{hostname}}:{{robot_port}}/api/robot/lock"
  body:
    type: json
    data: "{\r\n    \"vid\": \"ds1001\"\r\n}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
