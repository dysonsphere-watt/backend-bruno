info:
  name: Stop Lab Test
  type: http
  seq: 2

http:
  method: POST
  url: "{{protocol}}://{{hostname}}:{{robot_port}}/api/robot/:id/test/:test_id/stop"
  params:
    - name: id
      value: "5000"
      type: path
    - name: test_id
      value: "2"
      type: path
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
