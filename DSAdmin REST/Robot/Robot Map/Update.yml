info:
  name: Update
  type: http
  seq: 3

http:
  method: PUT
  url: "{{protocol}}://{{hostname}}:{{robot_port}}/api/robot/map/:id"
  params:
    - name: id
      value: "5"
      type: path
  body:
    type: json
    data: "{\r\n    \"status\": 1\r\n}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
