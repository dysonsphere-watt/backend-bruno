info:
  name: Query All
  type: http
  seq: 1

http:
  method: GET
  url: "{{protocol}}://{{hostname}}:{{robot_port}}/api/robot/maintenance/:maint-id/activity"
  params:
    - name: maint-id
      value: "1154"
      type: path
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
