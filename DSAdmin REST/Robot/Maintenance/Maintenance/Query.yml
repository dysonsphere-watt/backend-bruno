info:
  name: Query
  type: http
  seq: 4

http:
  method: GET
  url: "{{protocol}}://{{hostname}}:{{robot_port}}/api/robot/maintenance/:id"
  params:
    - name: id
      value: "10041"
      type: path
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
