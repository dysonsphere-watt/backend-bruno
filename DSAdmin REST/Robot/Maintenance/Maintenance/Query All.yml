info:
  name: Query All
  type: http
  seq: 5

http:
  method: GET
  url: "{{protocol}}://{{hostname}}:{{robot_port}}/api/robot/maintenance?order[m.id]=DESC"
  params:
    - name: order[m.id]
      value: DESC
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
