info:
  name: Update
  type: http
  seq: 2

http:
  method: PUT
  url: "{{protocol}}://{{hostname}}:{{robot_port}}/api/robot/maintenance/:maint-id/media/:media-id"
  params:
    - name: maint-id
      value: "8037"
      type: path
    - name: media-id
      value: "7"
      type: path
  body:
    type: multipart-form
    data:
      - name: status
        type: text
        value: "555"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
