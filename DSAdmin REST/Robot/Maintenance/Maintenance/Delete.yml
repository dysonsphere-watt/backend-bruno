info:
  name: Delete
  type: http
  seq: 6

http:
  method: DELETE
  url: "{{protocol}}://{{hostname}}:{{robot_port}}/api/robot/maintenance/2"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
