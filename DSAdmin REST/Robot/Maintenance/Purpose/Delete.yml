info:
  name: Delete
  type: http
  seq: 3

http:
  method: DELETE
  url: "{{protocol}}://{{hostname}}:{{robot_port}}/api/robot/maintenance/purpose/:id"
  params:
    - name: id
      value: "7"
      type: path
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
