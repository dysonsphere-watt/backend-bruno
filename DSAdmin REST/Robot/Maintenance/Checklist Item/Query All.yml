info:
  name: Query All
  type: http
  seq: 2

http:
  method: GET
  url: "{{protocol}}://{{hostname}}:{{robot_port}}/api/robot/maintenance/checklist-item"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
