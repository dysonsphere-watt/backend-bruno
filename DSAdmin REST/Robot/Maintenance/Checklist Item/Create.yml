info:
  name: Create
  type: http
  seq: 1

http:
  method: POST
  url: "{{protocol}}://{{hostname}}:{{robot_port}}/api/robot/maintenance/checklist-item"
  body:
    type: json
    data: "{\r

      \    \"type\": \"test delete\"\r

      }"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
