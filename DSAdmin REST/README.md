# Watt Backend API Requests

## JWT Auth

For requests that require the `Authorization: Bearer` token, it is stored in the `backend-env` environment under `auth`. This value is automatically set by the `User/User/Login` file's post response script.
