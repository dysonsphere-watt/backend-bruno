info:
  name: Health Check
  type: http
  seq: 2

http:
  method: GET
  url: http://{{hostname}}:{{log_port}}/api/log/healthcheck
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
