info:
  name: Sync
  type: http
  seq: 7

http:
  method: POST
  url: "{{protocol}}://{{hostname}}:{{ticketmaster_port}}/api/ticketmaster/ticket/:id/sync"
  params:
    - name: id
      value: "50019"
      type: path
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
