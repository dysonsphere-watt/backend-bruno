info:
  name: Query
  type: http
  seq: 3

http:
  method: GET
  url: "{{protocol}}://{{hostname}}:{{ticketmaster_port}}/api/ticketmaster/ticket/:id"
  params:
    - name: id
      value: "160033"
      type: path
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
