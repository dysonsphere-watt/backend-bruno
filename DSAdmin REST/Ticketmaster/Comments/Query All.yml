info:
  name: Query All
  type: http
  seq: 2

http:
  method: GET
  url: "{{protocol}}://{{hostname}}:{{ticketmaster_port}}/api/ticketmaster/comment/:parent-type/:parent-id"
  params:
    - name: parent-type
      value: "1"
      type: path
    - name: parent-id
      value: "80016"
      type: path
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
