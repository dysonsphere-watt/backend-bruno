info:
  name: Health Check
  type: http
  seq: 7

http:
  method: GET
  url: "{{protocol}}://{{hostname}}:{{ticketmaster_port}}/api/ticketmaster/healthcheck"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
