info:
  name: Create
  type: http
  seq: 1

http:
  method: POST
  url: "{{protocol}}://{{hostname}}:{{ticketmaster_port}}/api/ticketmaster/file/:parent-type/:parent-id"
  params:
    - name: parent-type
      value: "1"
      type: path
    - name: parent-id
      value: "3"
      type: path
  body:
    type: multipart-form
    data:
      - name: file_description
        type: text
        value: asodisah
      - name: file
        type: file
        value:
          - /C:/Users/dyson/Desktop/base-ad.mp4
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
