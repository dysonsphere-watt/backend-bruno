info:
  name: Health Check
  type: http
  seq: 4

http:
  method: GET
  url: "{{protocol}}://{{hostname}}:{{notification_port}}/api/notification/healthcheck"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
