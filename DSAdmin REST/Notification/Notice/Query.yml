info:
  name: Query
  type: http
  seq: 3

http:
  method: GET
  url: "{{protocol}}://{{hostname}}:{{notification_port}}/api/notification/notice/14011"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
