info:
  name: Query All
  type: http
  seq: 4

http:
  method: GET
  url: "{{protocol}}://{{hostname}}:{{notification_port}}/api/notification/notice?filter[type]=push"
  params:
    - name: filter[type]
      value: push
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
