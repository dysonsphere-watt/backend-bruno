info:
  name: Delete
  type: http
  seq: 5

http:
  method: DELETE
  url: "{{protocol}}://{{hostname}}:{{notification_port}}/api/notification/notice/20"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
