info:
  name: Mark
  type: http
  seq: 7

http:
  method: PUT
  url: "{{protocol}}://{{hostname}}:{{notification_port}}/api/notification/notice/:id/mark"
  params:
    - name: id
      value: "528"
      type: path
  body:
    type: json
    data: "{\r\n    \"is_read\": true\r\n}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
