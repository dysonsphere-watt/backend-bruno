info:
  name: HitPay Get Charges
  type: http
  seq: 3

http:
  method: GET
  url: https://api.sandbox.hit-pay.com/v1/charges/export
  headers:
    - name: X-BUSINESS-API-KEY
      value: "{{hitpay_api_key}}"
  body:
    type: json
    data: ""
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
