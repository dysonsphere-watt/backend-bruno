info:
  name: Get All Saved Cards
  type: http
  seq: 8

http:
  method: GET
  url: https://api.sandbox.hit-pay.com/v1/recurring-billing?customer_email=benjamin.goh@dysonsphere.com.sg
  headers:
    - name: X-BUSINESS-API-KEY
      value: "{{hitpay_api_key}}"
  params:
    - name: customer_email
      value: benjamin.goh@dysonsphere.com.sg
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
