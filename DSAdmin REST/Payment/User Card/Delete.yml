info:
  name: Delete
  type: http
  seq: 3

http:
  method: DELETE
  url: "{{protocol}}://{{hostname}}:{{payment_port}}/api/payment/user-card/4"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
