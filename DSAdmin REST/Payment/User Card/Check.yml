info:
  name: Check
  type: http
  seq: 5

http:
  method: GET
  url: "{{protocol}}://{{hostname}}:{{payment_port}}/api/payment/user-card/:id/check?last_four=4444"
  params:
    - name: last_four
      value: "4444"
      type: query
    - name: id
      value: "10"
      type: path
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
