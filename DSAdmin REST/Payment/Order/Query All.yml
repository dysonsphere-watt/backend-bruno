info:
  name: Query All
  type: http
  seq: 3

http:
  method: GET
  url: "{{protocol}}://{{hostname}}:{{payment_port}}/api/payment/order"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
