info:
  name: Delete
  type: http
  seq: 5

http:
  method: DELETE
  url: "{{protocol}}://{{hostname}}:{{payment_port}}/api/payment/order/150"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
