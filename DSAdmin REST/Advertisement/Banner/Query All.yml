info:
  name: Query All
  type: http
  seq: 5

http:
  method: GET
  url: "{{protocol}}://{{hostname}}:{{station_port}}/api/advertisement/banner"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
