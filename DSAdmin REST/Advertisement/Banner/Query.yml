info:
  name: Query
  type: http
  seq: 4

http:
  method: GET
  url: "{{protocol}}://{{hostname}}:{{station_port}}/api/advertisement/banner/:id"
  params:
    - name: id
      value: "2"
      type: path
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
