info:
  name: Delete
  type: http
  seq: 3

http:
  method: DELETE
  url: "{{protocol}}://{{hostname}}:{{station_port}}/api/advertisement/banner/:id"
  params:
    - name: id
      value: "66"
      type: path
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
