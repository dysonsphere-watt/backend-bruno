info:
  name: Get Media Info
  type: http
  seq: 9

http:
  method: GET
  url: "{{protocol}}://{{hostname}}:{{advertisement_port}}/api/advertisement/:id/media"
  params:
    - name: id
      value: "28"
      type: path
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
