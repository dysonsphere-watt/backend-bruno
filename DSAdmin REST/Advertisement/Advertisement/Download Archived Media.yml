info:
  name: Download Archived Media
  type: http
  seq: 13

http:
  method: GET
  url: "{{protocol}}://{{hostname}}:{{advertisement_port}}/api/advertisement/:id/media/archived"
  params:
    - name: id
      value: "96"
      type: path
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
