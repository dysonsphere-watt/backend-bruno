info:
  name: Delete Media
  type: http
  seq: 3

http:
  method: DELETE
  url: "{{protocol}}://{{hostname}}:{{advertisement_port}}/api/advertisement/:id/media"
  params:
    - name: id
      value: "94"
      type: path
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
