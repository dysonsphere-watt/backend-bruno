info:
  name: Update
  type: http
  seq: 7

http:
  method: PUT
  url: "{{protocol}}://{{hostname}}:{{advertisement_port}}/api/advertisement/:id"
  params:
    - name: id
      value: "86"
      type: path
  body:
    type: json
    data: "{\r\n    \"title\": \"nutitle\"\r\n}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
