info:
  name: Health Check
  type: http
  seq: 8

http:
  method: GET
  url: http://{{hostname}}:{{station_port}}/api/station/healthcheck
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
