info:
  name: Delete
  type: http
  seq: 2

http:
  method: DELETE
  url: "{{protocol}}://{{hostname}}:{{station_port}}/api/station/:id/banner/:b_id"
  params:
    - name: id
      value: "7037"
      type: path
    - name: b_id
      value: "1"
      type: path
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
