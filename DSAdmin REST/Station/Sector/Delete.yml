info:
  name: Delete
  type: http
  seq: 5

http:
  method: DELETE
  url: http://{{hostname}}:{{station_port}}/api/station/7000/sector/3
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
