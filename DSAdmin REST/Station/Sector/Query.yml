info:
  name: Query
  type: http
  seq: 2

http:
  method: GET
  url: "{{protocol}}://{{hostname}}:{{station_port}}/api/station/7005/sector/8"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
