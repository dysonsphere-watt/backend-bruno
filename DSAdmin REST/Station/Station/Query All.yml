info:
  name: Query All
  type: http
  seq: 11

http:
  method: GET
  url: "{{protocol}}://{{hostname}}:{{station_port}}/api/station"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
