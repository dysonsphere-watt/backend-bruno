info:
  name: Query
  type: http
  seq: 10

http:
  method: GET
  url: "{{protocol}}://{{hostname}}:{{station_port}}/api/station/22"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
