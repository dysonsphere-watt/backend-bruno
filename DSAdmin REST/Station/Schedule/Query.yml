info:
  name: Query
  type: http
  seq: 2

http:
  method: GET
  url: http://{{hostname}}:{{station_port}}/api/station/7000/schedule
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
