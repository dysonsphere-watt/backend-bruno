info:
  name: Delete
  type: http
  seq: 4

http:
  method: DELETE
  url: http://{{hostname}}:{{station_port}}/api/station/amenity/17
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
