info:
  name: Create
  type: http
  seq: 1

http:
  method: POST
  url: http://{{hostname}}:{{station_port}}/api/station/amenity
  body:
    type: json
    data: "{\r

      \    \"name\": \"{{$randomProduct}}\"\r

      }"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
