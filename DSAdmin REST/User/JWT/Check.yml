info:
  name: Check
  type: http
  seq: 1

http:
  method: POST
  url: http://{{hostname}}:{{user_port}}/api/user/jwt/check/
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
