info:
  name: Query
  type: http
  seq: 3

http:
  method: GET
  url: http://{{hostname}}:{{user_port}}/api/user/role/13
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
