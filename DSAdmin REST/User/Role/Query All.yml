info:
  name: Query All
  type: http
  seq: 4

http:
  method: GET
  url: http://{{hostname}}:{{user_port}}/api/user/role
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
