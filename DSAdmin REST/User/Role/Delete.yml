info:
  name: Delete
  type: http
  seq: 2

http:
  method: DELETE
  url: http://{{hostname}}:{{user_port}}/api/user/role/9
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
