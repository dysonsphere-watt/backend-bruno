info:
  name: Forgot
  type: http
  seq: 1

http:
  method: POST
  url: "{{protocol}}://{{hostname}}:{{user_port}}/api/user/password/forgot"
  body:
    type: json
    data: |-
      {
        "is_dashboard": true,
        "email": "benjamin.goh@dysonsphere.com.sg"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
