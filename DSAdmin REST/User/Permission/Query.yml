info:
  name: Query
  type: http
  seq: 2

http:
  method: GET
  url: http://{{hostname}}:{{user_port}}/api/user/permission
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
