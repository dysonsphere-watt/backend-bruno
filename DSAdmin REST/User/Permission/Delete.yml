info:
  name: Delete
  type: http
  seq: 3

http:
  method: DELETE
  url: http://{{hostname}}:{{user_port}}/api/user/permission/16
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
