info:
  name: Health Check
  type: http
  seq: 9

http:
  method: GET
  url: "{{protocol}}://{{hostname}}:{{user_port}}/api/user/healthcheck"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
