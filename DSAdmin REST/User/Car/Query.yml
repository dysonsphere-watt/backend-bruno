info:
  name: Query
  type: http
  seq: 3

http:
  method: GET
  url: "{{protocol}}://{{hostname}}:{{user_port}}/api/user/car"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
