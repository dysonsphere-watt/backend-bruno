info:
  name: Delete
  type: http
  seq: 4

http:
  method: DELETE
  url: http://{{hostname}}:{{user_port}}/api/user/car/7
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
