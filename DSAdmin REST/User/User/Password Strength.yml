info:
  name: Password Strength
  type: http
  seq: 16

http:
  method: POST
  url: "{{protocol}}://{{hostname}}:{{user_port}}/api/user/password/strength"
  body:
    type: json
    data: "{\r\n    \"password\": \"VV3ry\"\r\n}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
