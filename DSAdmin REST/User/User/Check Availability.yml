info:
  name: Check Availability
  type: http
  seq: 11

http:
  method: GET
  url: "{{protocol}}://{{hostname}}:{{user_port}}/api/user/availability?email=eveseow37@gmail.com&dashboard=true"
  params:
    - name: email
      value: eveseow37@gmail.com
      type: query
    - name: dashboard
      value: "true"
      type: query

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
