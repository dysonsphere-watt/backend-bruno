info:
  name: Query All
  type: http
  seq: 12

http:
  method: GET
  url: "{{protocol}}://{{hostname}}:{{user_port}}/api/user/all?page_size=8192&filter[role_id]=1"
  params:
    - name: page_size
      value: "8192"
      type: query
    - name: filter[role_id]
      value: "1"
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
