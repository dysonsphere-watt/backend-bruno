info:
  name: Update
  type: http
  seq: 7

http:
  method: PUT
  url: "{{protocol}}://{{hostname}}:{{user_port}}/api/user"
  body:
    type: multipart-form
    data:
      - name: full_name
        type: text
        value: new full name
      - name: date_of_birth
        type: text
        value: 2025-01-01
      - name: receive_marketing
        type: text
        value: "false"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
