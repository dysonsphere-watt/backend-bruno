info:
  name: Query
  type: http
  seq: 8

http:
  method: GET
  url: "{{protocol}}://{{hostname}}:{{user_port}}/api/user"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
