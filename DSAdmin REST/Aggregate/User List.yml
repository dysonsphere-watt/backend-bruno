info:
  name: User List
  type: http
  seq: 1

http:
  method: GET
  url: "{{protocol}}://{{hostname}}:{{station_port}}/api/aggr/user"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
