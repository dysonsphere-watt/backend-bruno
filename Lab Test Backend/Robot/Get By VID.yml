info:
  name: Get By VID
  type: http
  seq: 2

http:
  method: GET
  url: "{{protocol}}://{{hostname}}:{{port}}/api/v1/robot/:vid"
  params:
    - name: vid
      value: DS0001
      type: path
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
