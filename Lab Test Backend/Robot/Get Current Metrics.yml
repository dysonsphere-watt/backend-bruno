info:
  name: Get Current Metrics
  type: http
  seq: 3

http:
  method: GET
  url: "{{protocol}}://{{hostname}}:{{port}}/api/v1/robot/:vid/metrics/current"
  params:
    - name: vid
      value: DS0001
      type: path
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
