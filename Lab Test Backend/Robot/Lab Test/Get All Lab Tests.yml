info:
  name: Get All Lab Tests
  type: http
  seq: 4

http:
  method: GET
  url: "{{protocol}}://{{hostname}}:{{port}}/api/v1/robot/:vid/test"
  params:
    - name: vid
      value: ds0001
      type: path
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
