info:
  name: Stop Lab Test
  type: http
  seq: 2

http:
  method: POST
  url: "{{protocol}}://{{hostname}}:{{port}}/api/v1/robot/:vid/test/:lab_test_id/stop"
  params:
    - name: vid
      value: ds0003
      type: path
    - name: lab_test_id
      value: "753"
      type: path
  body:
    type: json
    data: "{\r\n    \"force\": true\r\n}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
