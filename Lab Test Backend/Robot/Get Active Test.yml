info:
  name: Get Active Test
  type: http
  seq: 5

http:
  method: GET
  url: "{{protocol}}://{{hostname}}:{{port}}/api/v1/robot/:vid/active-test"
  params:
    - name: vid
      value: ds9999
      type: path
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
