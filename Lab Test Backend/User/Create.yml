info:
  name: Create
  type: http
  seq: 2

http:
  method: POST
  url: "{{protocol}}://{{hostname}}:{{port}}/api/v1/user"
  body:
    type: json
    data: "{\r

      \    \"username\": \"testuser\",\r

      \    \"password\": \"password\",\r

      \    \"role_id\": 1\r

      }"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
